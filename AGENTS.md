# ELRSlang 项目协作规范

## 代码设计

- 写代码时优先保持清晰的架构边界和可复用性：让模块职责、数据流、pass/graph contract 和资源生命周期容易理解、测试和扩展。
- 在代码设计和运行效率之间做明确权衡：热路径、GPU 提交、资源上传、shader 调用等性能敏感处可以采用更直接的实现；非热路径优先选择可读、可维护、低耦合的结构。
- 新增抽象应服务于真实复用、复杂度降低或既有架构一致性；不要为了“看起来更架构化”引入额外层级。
- 修改性能相关逻辑时，尽量用测试、截图、timing 或像素对比验证收益，避免只凭主观判断保留实验性改动。
